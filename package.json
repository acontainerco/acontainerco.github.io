{
  "name": "acontainer.co",
  "private": true,
  "version": "1.0.0",
  "author": "Douglas Luman",
  "license": "Unlicense",
  "description": "Static site builder site for Container, a book arts press",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve --input=src --output=dist --formats=html,njk,gif,png,svg,jpg,jpeg,webp,css,js,md,pdf",
    "build": "npx @11ty/eleventy --input=src --output=dist --formats=html,njk,gif,png,svg,jpg,jpeg,webp,css,js,md,pdf"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^3.1.0",
    "@11ty/eleventy-plugin-webc": "^0.4.0"
  }
}
