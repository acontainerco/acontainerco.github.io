{
  "name": "acontainer.co",
  "private": true,
  "version": "1.0.0",
  "author": "Douglas Luman",
  "license": "Unlicense",
  "description": "Static site builder site for Container, a book arts press",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve --input=src --output=dist --formats=html,njk,gif,png,svg,jpg,webp,css,js,md",
    "build": "npx @11ty/eleventy --input=src --output=dist --formats=html,njk,gif,png,svg,jpg,webp,css,js,md"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
